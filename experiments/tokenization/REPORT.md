# Tokenization lab report

Status: in progress. This file is updated as runs complete.

## Setup

- Fake ledger: 1000 goods in the mous SQLite DB, served on `127.0.0.1:8000`.
- Gold labels computed from the live API export for reports / optimize / forecasts.
- GPT-5* token counts use tiktoken `o200k_base` (`encoding_for_model("gpt-5")`).

## Experiments

Filled in after the first token-count and gateway quality passes.

## Leaderboard

See `results/leaderboard.md` once generated.
